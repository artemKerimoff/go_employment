// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/example/micro/dismissal-order/ent/dismissalorderbody"
	"github.com/example/micro/dismissal-order/ent/dismissalorderheader"
	"github.com/example/micro/dismissal-order/ent/predicate"
)

// DismissalOrderHeaderQuery is the builder for querying DismissalOrderHeader entities.
type DismissalOrderHeaderQuery struct {
	config
	ctx        *QueryContext
	order      []dismissalorderheader.OrderOption
	inters     []Interceptor
	predicates []predicate.DismissalOrderHeader
	withBodies *DismissalOrderBodyQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DismissalOrderHeaderQuery builder.
func (dohq *DismissalOrderHeaderQuery) Where(ps ...predicate.DismissalOrderHeader) *DismissalOrderHeaderQuery {
	dohq.predicates = append(dohq.predicates, ps...)
	return dohq
}

// Limit the number of records to be returned by this query.
func (dohq *DismissalOrderHeaderQuery) Limit(limit int) *DismissalOrderHeaderQuery {
	dohq.ctx.Limit = &limit
	return dohq
}

// Offset to start from.
func (dohq *DismissalOrderHeaderQuery) Offset(offset int) *DismissalOrderHeaderQuery {
	dohq.ctx.Offset = &offset
	return dohq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dohq *DismissalOrderHeaderQuery) Unique(unique bool) *DismissalOrderHeaderQuery {
	dohq.ctx.Unique = &unique
	return dohq
}

// Order specifies how the records should be ordered.
func (dohq *DismissalOrderHeaderQuery) Order(o ...dismissalorderheader.OrderOption) *DismissalOrderHeaderQuery {
	dohq.order = append(dohq.order, o...)
	return dohq
}

// QueryBodies chains the current query on the "bodies" edge.
func (dohq *DismissalOrderHeaderQuery) QueryBodies() *DismissalOrderBodyQuery {
	query := (&DismissalOrderBodyClient{config: dohq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dohq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dohq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(dismissalorderheader.Table, dismissalorderheader.FieldID, selector),
			sqlgraph.To(dismissalorderbody.Table, dismissalorderbody.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, dismissalorderheader.BodiesTable, dismissalorderheader.BodiesColumn),
		)
		fromU = sqlgraph.SetNeighbors(dohq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DismissalOrderHeader entity from the query.
// Returns a *NotFoundError when no DismissalOrderHeader was found.
func (dohq *DismissalOrderHeaderQuery) First(ctx context.Context) (*DismissalOrderHeader, error) {
	nodes, err := dohq.Limit(1).All(setContextOp(ctx, dohq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{dismissalorderheader.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) FirstX(ctx context.Context) *DismissalOrderHeader {
	node, err := dohq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DismissalOrderHeader ID from the query.
// Returns a *NotFoundError when no DismissalOrderHeader ID was found.
func (dohq *DismissalOrderHeaderQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dohq.Limit(1).IDs(setContextOp(ctx, dohq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{dismissalorderheader.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) FirstIDX(ctx context.Context) int64 {
	id, err := dohq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DismissalOrderHeader entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DismissalOrderHeader entity is found.
// Returns a *NotFoundError when no DismissalOrderHeader entities are found.
func (dohq *DismissalOrderHeaderQuery) Only(ctx context.Context) (*DismissalOrderHeader, error) {
	nodes, err := dohq.Limit(2).All(setContextOp(ctx, dohq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{dismissalorderheader.Label}
	default:
		return nil, &NotSingularError{dismissalorderheader.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) OnlyX(ctx context.Context) *DismissalOrderHeader {
	node, err := dohq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DismissalOrderHeader ID in the query.
// Returns a *NotSingularError when more than one DismissalOrderHeader ID is found.
// Returns a *NotFoundError when no entities are found.
func (dohq *DismissalOrderHeaderQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dohq.Limit(2).IDs(setContextOp(ctx, dohq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{dismissalorderheader.Label}
	default:
		err = &NotSingularError{dismissalorderheader.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := dohq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DismissalOrderHeaders.
func (dohq *DismissalOrderHeaderQuery) All(ctx context.Context) ([]*DismissalOrderHeader, error) {
	ctx = setContextOp(ctx, dohq.ctx, "All")
	if err := dohq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DismissalOrderHeader, *DismissalOrderHeaderQuery]()
	return withInterceptors[[]*DismissalOrderHeader](ctx, dohq, qr, dohq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) AllX(ctx context.Context) []*DismissalOrderHeader {
	nodes, err := dohq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DismissalOrderHeader IDs.
func (dohq *DismissalOrderHeaderQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if dohq.ctx.Unique == nil && dohq.path != nil {
		dohq.Unique(true)
	}
	ctx = setContextOp(ctx, dohq.ctx, "IDs")
	if err = dohq.Select(dismissalorderheader.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) IDsX(ctx context.Context) []int64 {
	ids, err := dohq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dohq *DismissalOrderHeaderQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dohq.ctx, "Count")
	if err := dohq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dohq, querierCount[*DismissalOrderHeaderQuery](), dohq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) CountX(ctx context.Context) int {
	count, err := dohq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dohq *DismissalOrderHeaderQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dohq.ctx, "Exist")
	switch _, err := dohq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dohq *DismissalOrderHeaderQuery) ExistX(ctx context.Context) bool {
	exist, err := dohq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DismissalOrderHeaderQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dohq *DismissalOrderHeaderQuery) Clone() *DismissalOrderHeaderQuery {
	if dohq == nil {
		return nil
	}
	return &DismissalOrderHeaderQuery{
		config:     dohq.config,
		ctx:        dohq.ctx.Clone(),
		order:      append([]dismissalorderheader.OrderOption{}, dohq.order...),
		inters:     append([]Interceptor{}, dohq.inters...),
		predicates: append([]predicate.DismissalOrderHeader{}, dohq.predicates...),
		withBodies: dohq.withBodies.Clone(),
		// clone intermediate query.
		sql:  dohq.sql.Clone(),
		path: dohq.path,
	}
}

// WithBodies tells the query-builder to eager-load the nodes that are connected to
// the "bodies" edge. The optional arguments are used to configure the query builder of the edge.
func (dohq *DismissalOrderHeaderQuery) WithBodies(opts ...func(*DismissalOrderBodyQuery)) *DismissalOrderHeaderQuery {
	query := (&DismissalOrderBodyClient{config: dohq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dohq.withBodies = query
	return dohq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Number string `json:"number,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DismissalOrderHeader.Query().
//		GroupBy(dismissalorderheader.FieldNumber).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dohq *DismissalOrderHeaderQuery) GroupBy(field string, fields ...string) *DismissalOrderHeaderGroupBy {
	dohq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DismissalOrderHeaderGroupBy{build: dohq}
	grbuild.flds = &dohq.ctx.Fields
	grbuild.label = dismissalorderheader.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Number string `json:"number,omitempty"`
//	}
//
//	client.DismissalOrderHeader.Query().
//		Select(dismissalorderheader.FieldNumber).
//		Scan(ctx, &v)
func (dohq *DismissalOrderHeaderQuery) Select(fields ...string) *DismissalOrderHeaderSelect {
	dohq.ctx.Fields = append(dohq.ctx.Fields, fields...)
	sbuild := &DismissalOrderHeaderSelect{DismissalOrderHeaderQuery: dohq}
	sbuild.label = dismissalorderheader.Label
	sbuild.flds, sbuild.scan = &dohq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DismissalOrderHeaderSelect configured with the given aggregations.
func (dohq *DismissalOrderHeaderQuery) Aggregate(fns ...AggregateFunc) *DismissalOrderHeaderSelect {
	return dohq.Select().Aggregate(fns...)
}

func (dohq *DismissalOrderHeaderQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dohq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dohq); err != nil {
				return err
			}
		}
	}
	for _, f := range dohq.ctx.Fields {
		if !dismissalorderheader.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dohq.path != nil {
		prev, err := dohq.path(ctx)
		if err != nil {
			return err
		}
		dohq.sql = prev
	}
	return nil
}

func (dohq *DismissalOrderHeaderQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DismissalOrderHeader, error) {
	var (
		nodes       = []*DismissalOrderHeader{}
		_spec       = dohq.querySpec()
		loadedTypes = [1]bool{
			dohq.withBodies != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DismissalOrderHeader).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DismissalOrderHeader{config: dohq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dohq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dohq.withBodies; query != nil {
		if err := dohq.loadBodies(ctx, query, nodes,
			func(n *DismissalOrderHeader) { n.Edges.Bodies = []*DismissalOrderBody{} },
			func(n *DismissalOrderHeader, e *DismissalOrderBody) { n.Edges.Bodies = append(n.Edges.Bodies, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dohq *DismissalOrderHeaderQuery) loadBodies(ctx context.Context, query *DismissalOrderBodyQuery, nodes []*DismissalOrderHeader, init func(*DismissalOrderHeader), assign func(*DismissalOrderHeader, *DismissalOrderBody)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*DismissalOrderHeader)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(dismissalorderbody.FieldDismissalOrderHeaderID)
	}
	query.Where(predicate.DismissalOrderBody(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(dismissalorderheader.BodiesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.DismissalOrderHeaderID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "dismissal_order_header_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (dohq *DismissalOrderHeaderQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dohq.querySpec()
	_spec.Node.Columns = dohq.ctx.Fields
	if len(dohq.ctx.Fields) > 0 {
		_spec.Unique = dohq.ctx.Unique != nil && *dohq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dohq.driver, _spec)
}

func (dohq *DismissalOrderHeaderQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(dismissalorderheader.Table, dismissalorderheader.Columns, sqlgraph.NewFieldSpec(dismissalorderheader.FieldID, field.TypeInt64))
	_spec.From = dohq.sql
	if unique := dohq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dohq.path != nil {
		_spec.Unique = true
	}
	if fields := dohq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, dismissalorderheader.FieldID)
		for i := range fields {
			if fields[i] != dismissalorderheader.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dohq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dohq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dohq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dohq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dohq *DismissalOrderHeaderQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dohq.driver.Dialect())
	t1 := builder.Table(dismissalorderheader.Table)
	columns := dohq.ctx.Fields
	if len(columns) == 0 {
		columns = dismissalorderheader.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dohq.sql != nil {
		selector = dohq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dohq.ctx.Unique != nil && *dohq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dohq.predicates {
		p(selector)
	}
	for _, p := range dohq.order {
		p(selector)
	}
	if offset := dohq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dohq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DismissalOrderHeaderGroupBy is the group-by builder for DismissalOrderHeader entities.
type DismissalOrderHeaderGroupBy struct {
	selector
	build *DismissalOrderHeaderQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dohgb *DismissalOrderHeaderGroupBy) Aggregate(fns ...AggregateFunc) *DismissalOrderHeaderGroupBy {
	dohgb.fns = append(dohgb.fns, fns...)
	return dohgb
}

// Scan applies the selector query and scans the result into the given value.
func (dohgb *DismissalOrderHeaderGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dohgb.build.ctx, "GroupBy")
	if err := dohgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DismissalOrderHeaderQuery, *DismissalOrderHeaderGroupBy](ctx, dohgb.build, dohgb, dohgb.build.inters, v)
}

func (dohgb *DismissalOrderHeaderGroupBy) sqlScan(ctx context.Context, root *DismissalOrderHeaderQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dohgb.fns))
	for _, fn := range dohgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dohgb.flds)+len(dohgb.fns))
		for _, f := range *dohgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dohgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dohgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DismissalOrderHeaderSelect is the builder for selecting fields of DismissalOrderHeader entities.
type DismissalOrderHeaderSelect struct {
	*DismissalOrderHeaderQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dohs *DismissalOrderHeaderSelect) Aggregate(fns ...AggregateFunc) *DismissalOrderHeaderSelect {
	dohs.fns = append(dohs.fns, fns...)
	return dohs
}

// Scan applies the selector query and scans the result into the given value.
func (dohs *DismissalOrderHeaderSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dohs.ctx, "Select")
	if err := dohs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DismissalOrderHeaderQuery, *DismissalOrderHeaderSelect](ctx, dohs.DismissalOrderHeaderQuery, dohs, dohs.inters, v)
}

func (dohs *DismissalOrderHeaderSelect) sqlScan(ctx context.Context, root *DismissalOrderHeaderQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dohs.fns))
	for _, fn := range dohs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dohs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dohs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
