// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/example/micro/dismissal-order/ent/dismissalorderbody"
	"github.com/example/micro/dismissal-order/ent/dismissalorderheader"
	"github.com/example/micro/dismissal-order/ent/predicate"
)

// DismissalOrderBodyQuery is the builder for querying DismissalOrderBody entities.
type DismissalOrderBodyQuery struct {
	config
	ctx        *QueryContext
	order      []dismissalorderbody.OrderOption
	inters     []Interceptor
	predicates []predicate.DismissalOrderBody
	withHeader *DismissalOrderHeaderQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DismissalOrderBodyQuery builder.
func (dobq *DismissalOrderBodyQuery) Where(ps ...predicate.DismissalOrderBody) *DismissalOrderBodyQuery {
	dobq.predicates = append(dobq.predicates, ps...)
	return dobq
}

// Limit the number of records to be returned by this query.
func (dobq *DismissalOrderBodyQuery) Limit(limit int) *DismissalOrderBodyQuery {
	dobq.ctx.Limit = &limit
	return dobq
}

// Offset to start from.
func (dobq *DismissalOrderBodyQuery) Offset(offset int) *DismissalOrderBodyQuery {
	dobq.ctx.Offset = &offset
	return dobq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dobq *DismissalOrderBodyQuery) Unique(unique bool) *DismissalOrderBodyQuery {
	dobq.ctx.Unique = &unique
	return dobq
}

// Order specifies how the records should be ordered.
func (dobq *DismissalOrderBodyQuery) Order(o ...dismissalorderbody.OrderOption) *DismissalOrderBodyQuery {
	dobq.order = append(dobq.order, o...)
	return dobq
}

// QueryHeader chains the current query on the "header" edge.
func (dobq *DismissalOrderBodyQuery) QueryHeader() *DismissalOrderHeaderQuery {
	query := (&DismissalOrderHeaderClient{config: dobq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dobq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dobq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(dismissalorderbody.Table, dismissalorderbody.FieldID, selector),
			sqlgraph.To(dismissalorderheader.Table, dismissalorderheader.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, dismissalorderbody.HeaderTable, dismissalorderbody.HeaderColumn),
		)
		fromU = sqlgraph.SetNeighbors(dobq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DismissalOrderBody entity from the query.
// Returns a *NotFoundError when no DismissalOrderBody was found.
func (dobq *DismissalOrderBodyQuery) First(ctx context.Context) (*DismissalOrderBody, error) {
	nodes, err := dobq.Limit(1).All(setContextOp(ctx, dobq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{dismissalorderbody.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) FirstX(ctx context.Context) *DismissalOrderBody {
	node, err := dobq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DismissalOrderBody ID from the query.
// Returns a *NotFoundError when no DismissalOrderBody ID was found.
func (dobq *DismissalOrderBodyQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dobq.Limit(1).IDs(setContextOp(ctx, dobq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{dismissalorderbody.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) FirstIDX(ctx context.Context) int64 {
	id, err := dobq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DismissalOrderBody entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DismissalOrderBody entity is found.
// Returns a *NotFoundError when no DismissalOrderBody entities are found.
func (dobq *DismissalOrderBodyQuery) Only(ctx context.Context) (*DismissalOrderBody, error) {
	nodes, err := dobq.Limit(2).All(setContextOp(ctx, dobq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{dismissalorderbody.Label}
	default:
		return nil, &NotSingularError{dismissalorderbody.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) OnlyX(ctx context.Context) *DismissalOrderBody {
	node, err := dobq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DismissalOrderBody ID in the query.
// Returns a *NotSingularError when more than one DismissalOrderBody ID is found.
// Returns a *NotFoundError when no entities are found.
func (dobq *DismissalOrderBodyQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dobq.Limit(2).IDs(setContextOp(ctx, dobq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{dismissalorderbody.Label}
	default:
		err = &NotSingularError{dismissalorderbody.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := dobq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DismissalOrderBodies.
func (dobq *DismissalOrderBodyQuery) All(ctx context.Context) ([]*DismissalOrderBody, error) {
	ctx = setContextOp(ctx, dobq.ctx, "All")
	if err := dobq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DismissalOrderBody, *DismissalOrderBodyQuery]()
	return withInterceptors[[]*DismissalOrderBody](ctx, dobq, qr, dobq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) AllX(ctx context.Context) []*DismissalOrderBody {
	nodes, err := dobq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DismissalOrderBody IDs.
func (dobq *DismissalOrderBodyQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if dobq.ctx.Unique == nil && dobq.path != nil {
		dobq.Unique(true)
	}
	ctx = setContextOp(ctx, dobq.ctx, "IDs")
	if err = dobq.Select(dismissalorderbody.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) IDsX(ctx context.Context) []int64 {
	ids, err := dobq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dobq *DismissalOrderBodyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dobq.ctx, "Count")
	if err := dobq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dobq, querierCount[*DismissalOrderBodyQuery](), dobq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) CountX(ctx context.Context) int {
	count, err := dobq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dobq *DismissalOrderBodyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dobq.ctx, "Exist")
	switch _, err := dobq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dobq *DismissalOrderBodyQuery) ExistX(ctx context.Context) bool {
	exist, err := dobq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DismissalOrderBodyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dobq *DismissalOrderBodyQuery) Clone() *DismissalOrderBodyQuery {
	if dobq == nil {
		return nil
	}
	return &DismissalOrderBodyQuery{
		config:     dobq.config,
		ctx:        dobq.ctx.Clone(),
		order:      append([]dismissalorderbody.OrderOption{}, dobq.order...),
		inters:     append([]Interceptor{}, dobq.inters...),
		predicates: append([]predicate.DismissalOrderBody{}, dobq.predicates...),
		withHeader: dobq.withHeader.Clone(),
		// clone intermediate query.
		sql:  dobq.sql.Clone(),
		path: dobq.path,
	}
}

// WithHeader tells the query-builder to eager-load the nodes that are connected to
// the "header" edge. The optional arguments are used to configure the query builder of the edge.
func (dobq *DismissalOrderBodyQuery) WithHeader(opts ...func(*DismissalOrderHeaderQuery)) *DismissalOrderBodyQuery {
	query := (&DismissalOrderHeaderClient{config: dobq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dobq.withHeader = query
	return dobq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DismissalOrderHeaderID int64 `json:"dismissal_order_header_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DismissalOrderBody.Query().
//		GroupBy(dismissalorderbody.FieldDismissalOrderHeaderID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dobq *DismissalOrderBodyQuery) GroupBy(field string, fields ...string) *DismissalOrderBodyGroupBy {
	dobq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DismissalOrderBodyGroupBy{build: dobq}
	grbuild.flds = &dobq.ctx.Fields
	grbuild.label = dismissalorderbody.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DismissalOrderHeaderID int64 `json:"dismissal_order_header_id,omitempty"`
//	}
//
//	client.DismissalOrderBody.Query().
//		Select(dismissalorderbody.FieldDismissalOrderHeaderID).
//		Scan(ctx, &v)
func (dobq *DismissalOrderBodyQuery) Select(fields ...string) *DismissalOrderBodySelect {
	dobq.ctx.Fields = append(dobq.ctx.Fields, fields...)
	sbuild := &DismissalOrderBodySelect{DismissalOrderBodyQuery: dobq}
	sbuild.label = dismissalorderbody.Label
	sbuild.flds, sbuild.scan = &dobq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DismissalOrderBodySelect configured with the given aggregations.
func (dobq *DismissalOrderBodyQuery) Aggregate(fns ...AggregateFunc) *DismissalOrderBodySelect {
	return dobq.Select().Aggregate(fns...)
}

func (dobq *DismissalOrderBodyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dobq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dobq); err != nil {
				return err
			}
		}
	}
	for _, f := range dobq.ctx.Fields {
		if !dismissalorderbody.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dobq.path != nil {
		prev, err := dobq.path(ctx)
		if err != nil {
			return err
		}
		dobq.sql = prev
	}
	return nil
}

func (dobq *DismissalOrderBodyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DismissalOrderBody, error) {
	var (
		nodes       = []*DismissalOrderBody{}
		_spec       = dobq.querySpec()
		loadedTypes = [1]bool{
			dobq.withHeader != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DismissalOrderBody).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DismissalOrderBody{config: dobq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dobq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dobq.withHeader; query != nil {
		if err := dobq.loadHeader(ctx, query, nodes, nil,
			func(n *DismissalOrderBody, e *DismissalOrderHeader) { n.Edges.Header = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dobq *DismissalOrderBodyQuery) loadHeader(ctx context.Context, query *DismissalOrderHeaderQuery, nodes []*DismissalOrderBody, init func(*DismissalOrderBody), assign func(*DismissalOrderBody, *DismissalOrderHeader)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*DismissalOrderBody)
	for i := range nodes {
		fk := nodes[i].DismissalOrderHeaderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(dismissalorderheader.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "dismissal_order_header_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dobq *DismissalOrderBodyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dobq.querySpec()
	_spec.Node.Columns = dobq.ctx.Fields
	if len(dobq.ctx.Fields) > 0 {
		_spec.Unique = dobq.ctx.Unique != nil && *dobq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dobq.driver, _spec)
}

func (dobq *DismissalOrderBodyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(dismissalorderbody.Table, dismissalorderbody.Columns, sqlgraph.NewFieldSpec(dismissalorderbody.FieldID, field.TypeInt64))
	_spec.From = dobq.sql
	if unique := dobq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dobq.path != nil {
		_spec.Unique = true
	}
	if fields := dobq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, dismissalorderbody.FieldID)
		for i := range fields {
			if fields[i] != dismissalorderbody.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if dobq.withHeader != nil {
			_spec.Node.AddColumnOnce(dismissalorderbody.FieldDismissalOrderHeaderID)
		}
	}
	if ps := dobq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dobq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dobq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dobq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dobq *DismissalOrderBodyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dobq.driver.Dialect())
	t1 := builder.Table(dismissalorderbody.Table)
	columns := dobq.ctx.Fields
	if len(columns) == 0 {
		columns = dismissalorderbody.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dobq.sql != nil {
		selector = dobq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dobq.ctx.Unique != nil && *dobq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dobq.predicates {
		p(selector)
	}
	for _, p := range dobq.order {
		p(selector)
	}
	if offset := dobq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dobq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DismissalOrderBodyGroupBy is the group-by builder for DismissalOrderBody entities.
type DismissalOrderBodyGroupBy struct {
	selector
	build *DismissalOrderBodyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dobgb *DismissalOrderBodyGroupBy) Aggregate(fns ...AggregateFunc) *DismissalOrderBodyGroupBy {
	dobgb.fns = append(dobgb.fns, fns...)
	return dobgb
}

// Scan applies the selector query and scans the result into the given value.
func (dobgb *DismissalOrderBodyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dobgb.build.ctx, "GroupBy")
	if err := dobgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DismissalOrderBodyQuery, *DismissalOrderBodyGroupBy](ctx, dobgb.build, dobgb, dobgb.build.inters, v)
}

func (dobgb *DismissalOrderBodyGroupBy) sqlScan(ctx context.Context, root *DismissalOrderBodyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dobgb.fns))
	for _, fn := range dobgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dobgb.flds)+len(dobgb.fns))
		for _, f := range *dobgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dobgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dobgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DismissalOrderBodySelect is the builder for selecting fields of DismissalOrderBody entities.
type DismissalOrderBodySelect struct {
	*DismissalOrderBodyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dobs *DismissalOrderBodySelect) Aggregate(fns ...AggregateFunc) *DismissalOrderBodySelect {
	dobs.fns = append(dobs.fns, fns...)
	return dobs
}

// Scan applies the selector query and scans the result into the given value.
func (dobs *DismissalOrderBodySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dobs.ctx, "Select")
	if err := dobs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DismissalOrderBodyQuery, *DismissalOrderBodySelect](ctx, dobs.DismissalOrderBodyQuery, dobs, dobs.inters, v)
}

func (dobs *DismissalOrderBodySelect) sqlScan(ctx context.Context, root *DismissalOrderBodyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dobs.fns))
	for _, fn := range dobs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dobs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dobs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
