// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"github.com/example/micro/dismissal-order/ent/migrate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/example/micro/dismissal-order/ent/dismissalorderbody"
	"github.com/example/micro/dismissal-order/ent/dismissalorderheader"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// DismissalOrderBody is the client for interacting with the DismissalOrderBody builders.
	DismissalOrderBody *DismissalOrderBodyClient
	// DismissalOrderHeader is the client for interacting with the DismissalOrderHeader builders.
	DismissalOrderHeader *DismissalOrderHeaderClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	client := &Client{config: newConfig(opts...)}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.DismissalOrderBody = NewDismissalOrderBodyClient(c.config)
	c.DismissalOrderHeader = NewDismissalOrderHeaderClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("ent: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:                  ctx,
		config:               cfg,
		DismissalOrderBody:   NewDismissalOrderBodyClient(cfg),
		DismissalOrderHeader: NewDismissalOrderHeaderClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:                  ctx,
		config:               cfg,
		DismissalOrderBody:   NewDismissalOrderBodyClient(cfg),
		DismissalOrderHeader: NewDismissalOrderHeaderClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		DismissalOrderBody.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.DismissalOrderBody.Use(hooks...)
	c.DismissalOrderHeader.Use(hooks...)
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	c.DismissalOrderBody.Intercept(interceptors...)
	c.DismissalOrderHeader.Intercept(interceptors...)
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *DismissalOrderBodyMutation:
		return c.DismissalOrderBody.mutate(ctx, m)
	case *DismissalOrderHeaderMutation:
		return c.DismissalOrderHeader.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// DismissalOrderBodyClient is a client for the DismissalOrderBody schema.
type DismissalOrderBodyClient struct {
	config
}

// NewDismissalOrderBodyClient returns a client for the DismissalOrderBody from the given config.
func NewDismissalOrderBodyClient(c config) *DismissalOrderBodyClient {
	return &DismissalOrderBodyClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `dismissalorderbody.Hooks(f(g(h())))`.
func (c *DismissalOrderBodyClient) Use(hooks ...Hook) {
	c.hooks.DismissalOrderBody = append(c.hooks.DismissalOrderBody, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `dismissalorderbody.Intercept(f(g(h())))`.
func (c *DismissalOrderBodyClient) Intercept(interceptors ...Interceptor) {
	c.inters.DismissalOrderBody = append(c.inters.DismissalOrderBody, interceptors...)
}

// Create returns a builder for creating a DismissalOrderBody entity.
func (c *DismissalOrderBodyClient) Create() *DismissalOrderBodyCreate {
	mutation := newDismissalOrderBodyMutation(c.config, OpCreate)
	return &DismissalOrderBodyCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of DismissalOrderBody entities.
func (c *DismissalOrderBodyClient) CreateBulk(builders ...*DismissalOrderBodyCreate) *DismissalOrderBodyCreateBulk {
	return &DismissalOrderBodyCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *DismissalOrderBodyClient) MapCreateBulk(slice any, setFunc func(*DismissalOrderBodyCreate, int)) *DismissalOrderBodyCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &DismissalOrderBodyCreateBulk{err: fmt.Errorf("calling to DismissalOrderBodyClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*DismissalOrderBodyCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &DismissalOrderBodyCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for DismissalOrderBody.
func (c *DismissalOrderBodyClient) Update() *DismissalOrderBodyUpdate {
	mutation := newDismissalOrderBodyMutation(c.config, OpUpdate)
	return &DismissalOrderBodyUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *DismissalOrderBodyClient) UpdateOne(dob *DismissalOrderBody) *DismissalOrderBodyUpdateOne {
	mutation := newDismissalOrderBodyMutation(c.config, OpUpdateOne, withDismissalOrderBody(dob))
	return &DismissalOrderBodyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *DismissalOrderBodyClient) UpdateOneID(id int64) *DismissalOrderBodyUpdateOne {
	mutation := newDismissalOrderBodyMutation(c.config, OpUpdateOne, withDismissalOrderBodyID(id))
	return &DismissalOrderBodyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for DismissalOrderBody.
func (c *DismissalOrderBodyClient) Delete() *DismissalOrderBodyDelete {
	mutation := newDismissalOrderBodyMutation(c.config, OpDelete)
	return &DismissalOrderBodyDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *DismissalOrderBodyClient) DeleteOne(dob *DismissalOrderBody) *DismissalOrderBodyDeleteOne {
	return c.DeleteOneID(dob.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *DismissalOrderBodyClient) DeleteOneID(id int64) *DismissalOrderBodyDeleteOne {
	builder := c.Delete().Where(dismissalorderbody.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &DismissalOrderBodyDeleteOne{builder}
}

// Query returns a query builder for DismissalOrderBody.
func (c *DismissalOrderBodyClient) Query() *DismissalOrderBodyQuery {
	return &DismissalOrderBodyQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeDismissalOrderBody},
		inters: c.Interceptors(),
	}
}

// Get returns a DismissalOrderBody entity by its id.
func (c *DismissalOrderBodyClient) Get(ctx context.Context, id int64) (*DismissalOrderBody, error) {
	return c.Query().Where(dismissalorderbody.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *DismissalOrderBodyClient) GetX(ctx context.Context, id int64) *DismissalOrderBody {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryHeader queries the header edge of a DismissalOrderBody.
func (c *DismissalOrderBodyClient) QueryHeader(dob *DismissalOrderBody) *DismissalOrderHeaderQuery {
	query := (&DismissalOrderHeaderClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := dob.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(dismissalorderbody.Table, dismissalorderbody.FieldID, id),
			sqlgraph.To(dismissalorderheader.Table, dismissalorderheader.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, dismissalorderbody.HeaderTable, dismissalorderbody.HeaderColumn),
		)
		fromV = sqlgraph.Neighbors(dob.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *DismissalOrderBodyClient) Hooks() []Hook {
	return c.hooks.DismissalOrderBody
}

// Interceptors returns the client interceptors.
func (c *DismissalOrderBodyClient) Interceptors() []Interceptor {
	return c.inters.DismissalOrderBody
}

func (c *DismissalOrderBodyClient) mutate(ctx context.Context, m *DismissalOrderBodyMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&DismissalOrderBodyCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&DismissalOrderBodyUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&DismissalOrderBodyUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&DismissalOrderBodyDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown DismissalOrderBody mutation op: %q", m.Op())
	}
}

// DismissalOrderHeaderClient is a client for the DismissalOrderHeader schema.
type DismissalOrderHeaderClient struct {
	config
}

// NewDismissalOrderHeaderClient returns a client for the DismissalOrderHeader from the given config.
func NewDismissalOrderHeaderClient(c config) *DismissalOrderHeaderClient {
	return &DismissalOrderHeaderClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `dismissalorderheader.Hooks(f(g(h())))`.
func (c *DismissalOrderHeaderClient) Use(hooks ...Hook) {
	c.hooks.DismissalOrderHeader = append(c.hooks.DismissalOrderHeader, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `dismissalorderheader.Intercept(f(g(h())))`.
func (c *DismissalOrderHeaderClient) Intercept(interceptors ...Interceptor) {
	c.inters.DismissalOrderHeader = append(c.inters.DismissalOrderHeader, interceptors...)
}

// Create returns a builder for creating a DismissalOrderHeader entity.
func (c *DismissalOrderHeaderClient) Create() *DismissalOrderHeaderCreate {
	mutation := newDismissalOrderHeaderMutation(c.config, OpCreate)
	return &DismissalOrderHeaderCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of DismissalOrderHeader entities.
func (c *DismissalOrderHeaderClient) CreateBulk(builders ...*DismissalOrderHeaderCreate) *DismissalOrderHeaderCreateBulk {
	return &DismissalOrderHeaderCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *DismissalOrderHeaderClient) MapCreateBulk(slice any, setFunc func(*DismissalOrderHeaderCreate, int)) *DismissalOrderHeaderCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &DismissalOrderHeaderCreateBulk{err: fmt.Errorf("calling to DismissalOrderHeaderClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*DismissalOrderHeaderCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &DismissalOrderHeaderCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for DismissalOrderHeader.
func (c *DismissalOrderHeaderClient) Update() *DismissalOrderHeaderUpdate {
	mutation := newDismissalOrderHeaderMutation(c.config, OpUpdate)
	return &DismissalOrderHeaderUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *DismissalOrderHeaderClient) UpdateOne(doh *DismissalOrderHeader) *DismissalOrderHeaderUpdateOne {
	mutation := newDismissalOrderHeaderMutation(c.config, OpUpdateOne, withDismissalOrderHeader(doh))
	return &DismissalOrderHeaderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *DismissalOrderHeaderClient) UpdateOneID(id int64) *DismissalOrderHeaderUpdateOne {
	mutation := newDismissalOrderHeaderMutation(c.config, OpUpdateOne, withDismissalOrderHeaderID(id))
	return &DismissalOrderHeaderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for DismissalOrderHeader.
func (c *DismissalOrderHeaderClient) Delete() *DismissalOrderHeaderDelete {
	mutation := newDismissalOrderHeaderMutation(c.config, OpDelete)
	return &DismissalOrderHeaderDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *DismissalOrderHeaderClient) DeleteOne(doh *DismissalOrderHeader) *DismissalOrderHeaderDeleteOne {
	return c.DeleteOneID(doh.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *DismissalOrderHeaderClient) DeleteOneID(id int64) *DismissalOrderHeaderDeleteOne {
	builder := c.Delete().Where(dismissalorderheader.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &DismissalOrderHeaderDeleteOne{builder}
}

// Query returns a query builder for DismissalOrderHeader.
func (c *DismissalOrderHeaderClient) Query() *DismissalOrderHeaderQuery {
	return &DismissalOrderHeaderQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeDismissalOrderHeader},
		inters: c.Interceptors(),
	}
}

// Get returns a DismissalOrderHeader entity by its id.
func (c *DismissalOrderHeaderClient) Get(ctx context.Context, id int64) (*DismissalOrderHeader, error) {
	return c.Query().Where(dismissalorderheader.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *DismissalOrderHeaderClient) GetX(ctx context.Context, id int64) *DismissalOrderHeader {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// QueryBodies queries the bodies edge of a DismissalOrderHeader.
func (c *DismissalOrderHeaderClient) QueryBodies(doh *DismissalOrderHeader) *DismissalOrderBodyQuery {
	query := (&DismissalOrderBodyClient{config: c.config}).Query()
	query.path = func(context.Context) (fromV *sql.Selector, _ error) {
		id := doh.ID
		step := sqlgraph.NewStep(
			sqlgraph.From(dismissalorderheader.Table, dismissalorderheader.FieldID, id),
			sqlgraph.To(dismissalorderbody.Table, dismissalorderbody.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, dismissalorderheader.BodiesTable, dismissalorderheader.BodiesColumn),
		)
		fromV = sqlgraph.Neighbors(doh.driver.Dialect(), step)
		return fromV, nil
	}
	return query
}

// Hooks returns the client hooks.
func (c *DismissalOrderHeaderClient) Hooks() []Hook {
	return c.hooks.DismissalOrderHeader
}

// Interceptors returns the client interceptors.
func (c *DismissalOrderHeaderClient) Interceptors() []Interceptor {
	return c.inters.DismissalOrderHeader
}

func (c *DismissalOrderHeaderClient) mutate(ctx context.Context, m *DismissalOrderHeaderMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&DismissalOrderHeaderCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&DismissalOrderHeaderUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&DismissalOrderHeaderUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&DismissalOrderHeaderDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown DismissalOrderHeader mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		DismissalOrderBody, DismissalOrderHeader []ent.Hook
	}
	inters struct {
		DismissalOrderBody, DismissalOrderHeader []ent.Interceptor
	}
)
